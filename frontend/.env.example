# Copy to .env for local dev, or set as a Vercel project env var for
# deployments. Unset locally, this defaults to http://localhost:5000 (see
# src/api/client.js), so most local dev setups don't need this file at all.
VITE_API_BASE_URL=https://your-backend.onrender.com
